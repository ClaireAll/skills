interface:
  display_name: "转测前自查"
  short_description: "按本次迭代生成手动自查用例，覆盖业务边界、历史风险与关联回归"
  default_prompt: "使用 $test-skill，结合本次迭代的需求、交互和代码改动，按 P0 冒烟、P1 定向回归、P2 探索性检查组织手动转测自查用例，复用共享前置条件，并列出待确认、阻塞与未覆盖项；只生成用例，不执行测试。"
